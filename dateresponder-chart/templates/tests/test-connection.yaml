apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dateresponder-chart.fullname" . }}-test-connection"
  labels:
    {{- include "dateresponder-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dateresponder-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
