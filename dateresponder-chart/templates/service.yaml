apiVersion: v1
kind: Service
metadata:
  name: {{ include "dateresponder-chart.fullname" . }}
  labels:
    {{- include "dateresponder-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dateresponder-chart.selectorLabels" . | nindent 4 }}
