apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "datecaller-chart.fullname" . }}-test-connection"
  labels:
    {{- include "datecaller-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "datecaller-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
