apiVersion: v1
kind: Service
metadata:
  name: {{ include "datecaller-chart.fullname" . }}
  labels:
    {{- include "datecaller-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "datecaller-chart.selectorLabels" . | nindent 4 }}
